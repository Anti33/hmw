def smallest_multiple():
    
    def divisible(x):
        for i in range(2, 21): 
            if (x % i) != 0:
                return(False)
        return(True)
    
    i = 20

    counter = 0
    
    while True:
        if divisible(i) == True:
            return(i)
        else:
            i += 20
    return(nums)
    


print(smallest_multiple())

# answer is 232792560
